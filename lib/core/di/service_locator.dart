import 'package:get_it/get_it.dart';

final getIt = GetIt.instance;

void setupServiceLocator() {
  // Services will be registered by feature modules
  // Core services registration typically happens in main.dart
  // after Flutter bindings are initialized
}
